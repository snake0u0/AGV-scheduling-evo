# test-paper-example.py 파악

> 정리:

- build 함수: Instance 클래스와 Soution 클래스를 이용해서 문제 인스턴스와 해를 만듬.
- main 함수: simulate 함수를 이용해서 instance를 계산함. simulate함수는 계산 후 결과를 표로 출력함. 그리고 해와 비교 후 같으면 pass/ 아니면 FAIL을 출력함.

## 의문사항들